{% extends 'base.html.twig' %}

{% block title %}Delivery{% endblock %}

{% block body %}
    <h1>Delivery</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ delivery.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ delivery.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ delivery.type }}</td>
            </tr>
            <tr>
                <th>DateDelivery</th>
                <td>{{ delivery.dateDelivery ? delivery.dateDelivery|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_delivery_index') }}">back to list</a>

    <a href="{{ path('app_delivery_edit', {'id': delivery.id}) }}">edit</a>

    {{ include('delivery/_delete_form.html.twig') }}
{% endblock %}
